import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ApiService } from '../../core/api.service';
import { UiService } from '../../core/ui.service';
import { PageComponent } from '../../shared/page.component';
@Component({standalone:true,imports:[FormsModule,PageComponent],template:`<app-page eyebrow="AUTHENTICATION" title="Identity Providers" description="Configure and test external OIDC / SAML identity providers."><button actions class="primary" (click)="open=true">+ Add provider</button></app-page><div class="panel table-wrap"><table><thead><tr><th>Provider</th><th>Type</th><th>Issuer</th><th>Status</th><th></th></tr></thead><tbody>@for(p of rows;track p.id||p.identity_provider_id){<tr><td><strong>{{p.provider_name||p.providerName}}</strong></td><td>{{p.provider_type||p.providerType}}</td><td>{{p.issuer_url||p.issuerUrl}}</td><td><span class="badge" [class.good]="p.status==='ACTIVE'">{{p.status}}</span></td><td><button class="link" (click)="test(p)">Test</button></td></tr>}@empty{<tr><td colspan="5" class="empty">No identity providers returned.</td></tr>}</tbody></table></div>@if(open){<div class="modal-backdrop"><div class="modal"><h3>Add identity provider</h3><div class="form-grid"><label>Name<input [(ngModel)]="form.providerName"></label><label>Type<select [(ngModel)]="form.providerType"><option>OIDC</option><option>SAML</option></select></label><label>Issuer URL<input [(ngModel)]="form.issuerUrl"></label><label>Client ID<input [(ngModel)]="form.clientId"></label></div><div class="modal-actions"><button class="secondary" (click)="open=false">Cancel</button><button class="primary" (click)="create()">Save provider</button></div></div></div>}`})
export class IdpComponent {rows:any[]=[];open=false;form:any={providerName:'',providerType:'OIDC',issuerUrl:'',clientId:''};constructor(private api:ApiService,private ui:UiService){this.load()}load(){this.api.get<any>('/identity-providers',{page:1,limit:100}).subscribe({next:r=>this.rows=r?.data?.items||r?.items||r?.data||[]})}create(){this.api.post('/identity-providers',this.form).subscribe({next:()=>{this.open=false;this.ui.show('Identity provider created');this.load()}})}test(p:any){this.api.post(`/identity-providers/${p.id||p.identity_provider_id}/test`).subscribe({next:()=>this.ui.show('Provider connectivity test completed')})}}

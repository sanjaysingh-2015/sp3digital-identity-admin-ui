import { Component } from '@angular/core';
import { ApiService } from '../../core/api.service';
import { PageComponent } from '../../shared/page.component';
@Component({standalone:true,imports:[PageComponent],template:`<app-page eyebrow="APPLICATIONS" title="OAuth / OIDC Clients" description="Registered OAuth clients, redirect URIs, lifecycle and secret rotation."/><div class="panel table-wrap"><table><thead><tr><th>Client</th><th>Type</th><th>Grant types</th><th>Status</th><th>Expires</th><th>Actions</th></tr></thead><tbody>@for(c of rows;track c.id||c.client_id){<tr><td><strong>{{c.client_name||c.clientName}}</strong></td><td>{{c.client_type||c.clientType}}</td><td>{{(c.grant_types||c.grantTypes||[]).join(', ')}}</td><td><span class="badge good">{{c.status}}</span></td><td>{{c.expires_on||c.expiresOn||'—'}}</td><td><button class="link" (click)="rotate(c)">Rotate secret</button></td></tr>}@empty{<tr><td colspan="6" class="empty">No OAuth clients returned.</td></tr>}</tbody></table></div>`})
export class OAuthComponent {rows:any[]=[];constructor(private api:ApiService){this.load()}load(){this.api.get<any>('/oauth/clients',{page:1,limit:100}).subscribe({next:r=>this.rows=r?.data?.items||r?.items||r?.data||[]})}rotate(c:any){this.api.post(`/oauth/clients/${c.id||c.client_id}/rotate-secret`).subscribe()}}
